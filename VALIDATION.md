# Codex 配额管家 0.4.1 发布验证（2026-09-10）

- 完整 Python 回归 266 passed（162.83 秒），UTF-8 构建环境。新增设备顺序完整性/账号隔离、不增长日志免写与增长/半行/去重、组件固定路径/原子更新/哈希校验回归。
- 后台真实进程采样将主要 CPU 热点定位到 Python，WebView2 与 tsnet 基本空闲；冻结副本六阶段 CPU 中位 906.25→343.75 ms（降低 62.1%），wall 972.75→343.72 ms（降低 64.7%）。没有改同步周期、数据归属或额度算法；不采用跨轮归属缓存。
- 冻结副本的完整归属结果和 2674 条 outbox(payload/sent)在优化前后相同。测试覆盖正常与 runtime-only 两种核对路径；日志增长、半行补齐与重复扫描保持数值一致。
- 用户列表短按设置/拖动排序、备注/8色预设、在线状态与活动状态分离、短活动徽章、模型数字版本排序及隐藏自动审查选项保留总量、tooltip避峰均有前端回归。官方剩余值颜色边界与刷新倒计时另有断言。
- 固定 Tailscale helper 路径为数据目录 runtime/cqg-tsnet.exe，启动前核验来源和缓存 SHA256；系统已保存的临时路径防火墙规则不再作为新版本运行路径。首次系统授权仍可能需要管理员，未改全局防火墙规则。
- 继续复用已验证的 Tailscale 原生二进制，本轮 Go 源码未改、未重编。主程序、更新与恢复工具重新冻结；签名公钥身份保持一致。
- 冻结代码/前端资源/图标/原生与更新 helper 内容匹配源码；隔离冻结启动健康回执及正常退出通过。前端 8 项数据测试与完整 headless UI 回归通过。
- 双端本版本的真实升级后收敛与实际屏幕 FPS 未实测；同步界面以对端实际回执显示状态。

---

# Codex 配额管家 0.4.0 WebView2 本地试用验证（2026-09-10）

- 生产入口已切换为 Vue 3 + pywebview 6.2.1 / WebView2。冻结 EXE 不含 Tkinter/CustomTkinter；Python 账本、采集、模型权重和加密同步仍由原 Engine 负责。
- 完整 Python 回归：261 passed（202.11 秒，按项目构建配置启用 UTF-8）。第一轮未启用构建脚本的 PYTHONUTF8=1，260 passed、1 项测试因默认 GBK 读取 UTF-8 设置失败；该项在正确环境下单独复测通过。
- 前端数据测试 4 项通过；独立 headless Edge 组件测试通过。750×570 下两台设备完整可见；连续 20 次相同快照零 SVG 变更，菜单和设备选择不丢失；各页面、模型/用户筛选、柱状切换、预设颜色及设置操作参数验证通过。
- Windows 原生宿主探针确认内容区 750×570；启动、最小化、从任务栏恢复、隐藏到托盘与恢复的透明度过渡通过。原生拖动不再通过 Python 逐点移动，但没有操纵真实鼠标来验收手感。
- WebView2 图形探针确认 ANGLE / NVIDIA RTX 3080 Ti / Direct3D11。实际屏幕呈现 FPS 未测量，不能把 rAF 回调当作显示帧率。
- 三轮同账本离屏性能测试：空闲私有内存 90.9→291.4 MiB；折线/柱状悬停全进程 CPU 分别增加约 136.4% / 98.8%。新版动画时钟间隔中位 6.9 ms；资源开销增加，不能宣称全面降耗。详见 docs/性能实测-0.4.0.md 与 docs/performance-baseline.md。
- 冻结主程序入口、业务模块、全部前端资源、图标、tsnet 与更新 helper 的字节内容匹配源码/资产；冻结 EXE 隔离后台启动取得 0.4.0 健康回执，并正常退出。
- 本周期饼图和设备列表使用同一份周期 Token；历史累计单独标识。小时/日/周/月图表仍为已同步原始日志 Token；估算额度才使用模型权重。已移除设备不进入图例；账号/周期边界、重复与迟到记录保留回归覆盖。
- 同步状态依据账本进度，最近同步小字依据实际确认回执，不能由界面刷新时间替代。两个远端实体设备的本版本端到端验收未执行；不能保证远端当下已收齐数据。
- 本地试用不改自启动路径，不覆盖 A:\工具\额度监测 的安装版；本次未发布 GitHub Release 或签名自动更新。启动原账本前备份设置与 SQLite 数据库。

---

## 之前的 Tk 试用构建验证（保留历史记录，非当前生产渲染器）

# Codex 配额管家 0.4.0 本地改版验证记录

- 2026-09-10 界面性能修订：完整回归 242 passed（171.77 秒）。同步状态卡仅在账本进度确认后显示已同步，不能用连接在线替代。
- `tests/compact_ui_probe.py` 使用隔离数据验证真实 Tk 控件：模型和用户筛选、时间范围、曲线／柱状切换、个人额度口径、备注、空状态、蓝色活动状态、窗口缩放和最小化恢复。按钮文字中心偏差不超过 1.5 像素；曲线悬停点按中间帧移动。
- 默认 750 × 570 布局下，两台放大后的设备行在概览可见区域内部，概览已取消滚动条；设备配额和显示口径在设置中，第二卡片显示同步状态。图表只显示当前设备原始 Token，移除设备不再进入图例或用户筛选。
- 用户颜色改为 8 种预设；饼图、该用户曲线／柱状及表格数值一致，DPAPI 设置读回确认颜色按账号保存。图表不再提供加权用量选项，既有估算分摊算法保留。
- 性能复现：20 次相同视图刷新从 0.929 秒降至 0.103 秒；模型下拉菜单重复重建从 20 次降为 0 次，主要控件重复绘制被消除。该对比是隔离测试，不代表所有机器上的帧率。
- 真实控件验证：拖动窗口不重新栅格化图表；弹出下拉在 20 次后台视图刷新后保持同一窗口；切页滑入、下拉淡入、最小化和关闭到托盘淡出、恢复淡入均完成。窗口设置 DWM 圆角并完成截图检查。实际鼠标手感与双机使用仍由用户试用验收。
- 最终冻结 EXE：入口和业务模块字节码、图标、原生 helper 与更新 helper 均匹配当前源码／资产；隔离启动健康回执为 0.4.0，4 秒后正常退出。试用可用 `--no-autostart`，不改变原自启动路径。
- 本机真实账本以 SQLite `mode=ro` 读取：总使用、小时、24 小时、7 天、30 天的 Token 汇总分别与直接 SQL 一致，并完成真实数据界面预览。未修改生产账本或生产设置。
- 双账本通过原有 Journal 同步，验证重复、乱序及迟到历史后，5 个时间范围 × 2 种计量 × 3 个模型范围 × 3 个用户范围结果一致。边界、账号隔离、未知权重回退和个人配额分母另有回归。
- Token 为已同步日志记录；分设备官方额度占用仍按既有模型权重估算，不能视为官方逐设备账单。两个真实远端设备的升级后同步验收尚未执行；不能从本机测试推断远端当下已收齐数据。
- 本次保留并核对既有 tsnet 原生组件，不重新编译 Go helper。主程序、托盘及 EXE 图标统一；安装目录中的旧版未被替换，也未发布自动更新。

---

# Codex 配额管家 0.3.2 验证记录

- 正式构建完整回归 **226 passed**（200.47 秒），Go helper 测试与冻结代码/原生组件/许可一致性检查通过。
- 正式 EXE 演示及未添加账号后台启动通过；签名更新替换/重启、失败回滚及 frozen onefile 更新探针通过。
- 授权链接探针首次 25 秒超时；对相同冻结 helper 单独复测，4 秒内取得合法授权链接。未实际授权新节点。

- 新增周期重算、加密分页收敛、基线保留、周期重置、未知权重与重算后解除限制回归。
- 本机账本只读副本：30% 已用从 27.1027% / 2.8973% 重算为 25.2435% / 4.7565%，Token 不变。这是采样时估算，不是官方逐设备账单。
- 待归属诊断增加原因计数及事件时间范围；未放宽账号或 API 隔离，不宣称已解决远端 13 条待归属记录。
- 候选 EXE 冻结代码在真实副本上与源码计算一致；隔离演示与未添加账号启动通过。
- 正式构建、冻结包、更新回滚与远端验证结果见 v0.3.2 Release。尚未完成新分摊规则在两台物理设备上的升级后验收。

---

# Codex 配额管家 0.3.1 验证记录

- 正式构建完整回归 213 passed（229.18 秒，`work/build-release-0.3.1.log`）；真实 Windows helper 1,200 次状态／收件请求无重置。
- 换账号操作默认取消，确认后只执行内嵌节点 logout/login；保留组密钥和设备 ID，清除旧匹配码显示及地址缓存。
- 已验证本机协议修复和自动化行为；未完成两台异地机器的切换账号后端到端验收。
- 正式构建及更新发布检查结果见 v0.3.1 Release 说明。

---

# Codex 配额管家 0.3.0 验证记录

## 0.3.0 正式发布验证（2026-09-08）

- 正式构建完整回归：212 passed（204.53 秒）；Go helper 测试通过。构建日志：`work/build-release-0.3.0.log`。

- 新增组成员移除／重新输码加入、离线状态收敛、旧记录不覆盖新状态、默认取消的二次确认、总览删除行且不改历史账本的回归测试。
- 本机完成真实 Tailscale 授权，确认 Running、地址分配与身份持久化。真实 helper 的 600 次 IPC 压测复现连接重置；修复后同样请求量自动恢复且节点 PID 不变。
- 最终构建、打包一致性、授权链接、更新回滚的详细结果见本次发布说明。
- 未宣称完成长期异地同步、网络切换或多台真实设备成员管理验收。

## 0.3.0 早期候选：内嵌 Tailscale 与轻量化（2026-09-08）

- 完整回归：204 passed（`work/build-0.3.0-final.log`），包含原统计/账号隔离回归和新增加密握手、学习回程地址、双通信线程传输与重启、路径标签、旧数据保留和队列上限测试。双通信线程测试使用内存网络替身，不冒充公网测试。
- Go helper 固定 `tailscale.com v1.102.3`，`go.sum` 锁定依赖；原生构建及 IPC 拒绝匿名访问、消息上限/背压测试通过。
- 实际 Windows helper 在隔离目录启动，未安装系统 Tailscale，取得官方浏览器授权 URL，关闭后进程退出；未执行账号授权或注册完成。结果：`work/tsnet-smoke-92c4d26a/result.json`。日志不包含实际授权 URL。
- GUI 实际控件测试：CQG4 生成、待授权、直连、DERP 区域显示通过；已查看 `work/ui-preview-0.3.0/` 下窗口截图并修正总览连接列换行。所有连通状态截图均为演示，不是异地实测。
- 冻结包测试使用隔离数据目录：演示启动/退出、未添加账号的后台启动/退出、从主 EXE 提取的 tsnet helper 获取官方授权链接通过；未操作真实登录、限额或自启动。
- 更新助手在隔离临时 EXE 上验证签名替换/重启、失败回滚及 onefile 更新流程：`work/update-probe-0.3.0.log`。onefile 兼容输入沿用旧版 0.2.2，不声称已做 0.3.0 在线升级。
- 原 v0.2.6 GitHub Release 主程序为 85,818,046 字节（81.84 MiB）；新版约 41.35 MiB，约减小 49.5%。主包移除 Syncthing、WebRTC/PyAV/FFmpeg，内置 tsnet helper 约 20.97 MiB（主包内压缩后约 8.16 MiB）。更新助手仍保留完整签名/回滚能力。
- 保留原组密钥与设备 ID 时，Journal 路径和历史不变；新 UI 要求双方升级并授权、刷新 CQG4，不再连接旧公共中转。源码中的旧传输只保留作历史回归，不打入生产传输路径。
- **未完成：真实两台异地机器授权后的同步、直连与 DERP 路径切换、长期稳定性和实际历史收敛。** 本轮没有发布、推送、替换本机在用 EXE、改变路由器或防火墙设置。

---

以下为历史版本原始验证记录。

日期：2026-09-07。环境：当前 Windows 主机，Python 3.13，PyInstaller EXE。以下保留各历史阶段的原始验证记录。

## 0.2.6 同步与状态验证（2026-09-08）

- 两张同时期截图复现双方在线但设备 Token 只同步了一部分：橙猫猫侧为本机 10.08 M、对端 0；对端侧为本机 8.15 M、橙猫猫 2.72 M。稍后账本收敛为橙猫猫约 10.94 M、对端约 8.15 M，证明问题是历史分页收敛慢，不是永久丢失。
- 只读检查实际三设备账本，三个来源序列均连续。旧 24 KB / 60 条分页需要约 147 轮；新文件通道受 160 KB 明文、240 KiB 加密邮箱上限约束，需要约 21 轮。包含 CJK 设备名的边界测试验证加密包未超限。
- 新增同步大批次、收发上限、额度快照去重、本机与托盘活动轮询、当前日志活动类型和 Token 显示口径回归测试。修正 CJK 数据受字节上限提前截断的测试夹具预期后，完整回归 **198 passed（154.56 秒）**。
- 活动类型检查只读取结构化事件类型和时间，不读取或保存对话正文。真实异地状态的最终延迟仍受 Syncthing 中转与双方网络影响。

## 0.2.5 监测中断修复（2026-09-08）

- 使用发生故障的本机数据库副本复现 `HistoryRecovery.reconcile → RuntimeEvidence.resolve → IndexError`；修复后同一副本成功完成补记。
- 新增 6 项回归测试；修复前 5 项失败、1 项通过，修复后全部通过。完整测试 190 passed（Python 3.12，UTF-8 模式），包括本机多实例同步测试。
- 修复代码打包安装后，运行数据库恢复更新官方额度快照、本机在线状态和活动会话；用户确认本机已修好。
- 真实异地设备尚未完成双方升级后的连接验收。本机修复不能证明对端软件正在运行，也不能替代对端修复。
- 迁移导入原签名身份后，确认公钥匹配且 DPAPI 加解密验证通过；沿用原公钥生成并验证 0.2.5 签名更新清单。完整发布构建再次通过 190 项测试（158.83 秒），冻结代码、内置组件和许可证校验通过。

## 0.2.4 正式发布构建（2026-09-07）

- `build.ps1` 重新执行完整回归：**184 passed，206.43 秒**。重建主 EXE、内置更新助手、独立恢复工具与可选中转服务，未覆盖本机正在运行的安装位置。
- `scripts/verify_package.py` 验证冻结代码与当前源码一致，内置 Syncthing 哈希、更新助手和许可证匹配；真实 GUI 演示交互与截图检查通过。
- `tests/packaged_probe.py`：正式 EXE 演示启动／退出、未添加账号后台启动、隔离已添加账号只读额度查询、打包中转加密传输全部通过。
- `tests/windows_update_probe.py`：隔离临时 EXE 的签名替换／重启、失败回滚、PyInstaller onefile 退出后替换均通过；不替换用户安装。这一兼容性脚本的 onefile 输入仍为旧版 0.2.2。
- 正式主 EXE SHA256：`ed687c11865efda8e2d125d85d022692e4ba56a6d869841b1f412f441bbc38c9`。

## 0.2.4 Windows 配对兼容验证（2026-09-07）

- 用户异常栈指向 `FileMesh._write -> atomic_json -> Path.write_text`，失败握手临时文件路径为 267 字符。通过未启用 longPathAware 的真实 Windows 测试 EXE 复现：255 字符读写通过，267 字符出现相同 `FileNotFoundError/errno=2`。
- 仅为本机密文邮箱读写使用 Windows 扩展长度路径，保留原文件名、目录与协议。修复后的同条件 EXE 在两种长度下均完成 hello、facts、sync 写入和接收；另有盘符、UNC 及已带扩展前缀的回归测试。
- 用户随后提供的三设备列表截图显示公共加密中转连接。现场也曾出现独立的 `WinError 5` 弹窗，尚未得到其异常栈，不能声称权限报错根因已修复或全部历史已经同步。
- 发布包不包含临时 trace hook、诊断日志或测试版自动退出参数；保留其他任务完成的 0.2.4 运行实例归属、历史恢复与 Token 卡片实现。

## 0.2.2 正式构建验证（2026-09-06）

- 发布构建重新执行完整 121 项回归，全部通过（157.28 秒）；GUI 预览交互检查通过。
- 冻结主程序中模块 code object 与当前源文件一致，内置 Syncthing、更新助手和许可证验证通过。
- 新 EXE 演示启动/退出、未添加账号后台启动、已添加真实账号的只读额度查询，以及打包中转的加密传输通过；正式用户数据目录未修改。
- 更新助手在隔离测试 EXE 上完成签名替换/重启、失败回滚，以及真实 PyInstaller onefile 主程序退出后的替换验证。
- 使用既有 Ed25519 发布密钥签署 0.2.2 更新清单，兼容旧版检测更新；未主动替换本机运行中的程序。
- 主 EXE SHA256：`f6622d9f94d5ca160ef66c1e33e7dc4eecc29e22b3a04b9d5321528ec5dab26c`。

## 开发阶段：首次匹配稳定性与速度（2026-09-06）

- 最终完整测试集 121 项通过（152.22 秒）。新增失败先行的回归覆盖：早到 hello 不需等待第二次握手、目录共享写入失败后的幂等修复、后台初次连接/历史 ACK/版本向量进度唤醒、重复心跳不形成 ACK 循环、同组活动通道复用、短暂查询错误不重建进程而连续错误仍重试、已有设备的过期中转提示更新。
- 三个真实 Syncthing + Engine 实例分别验证前台与后台：后台首台预置 125 条历史记录，超过两批，传输等待期间没有测试代码强制唤醒；Token、配额与第三实例重启后同步通过。连接仅用隔离 loopback，不等价于三台异地机器。
- 同机隔离合成账号经真实公共中转，使用 CQG2 匹配码、AES-GCM 邮箱与持久重启验证。准备开始至匹配码就绪/双方设备就绪/首次数据到达，基线分别为 21.680/29.188/31.568 秒，修复后两次分别为 19.723/26.227/29.153 秒与 14.919/21.428/24.244 秒。小样本含公网波动，不作为固定性能承诺。
- 首次数据验证结束后执行接收端关闭/重启/再次收取数据，该阶段基线约 20.057 秒，两次修复后约 5.047/5.019 秒。
- 本地原始结果：`work/public-mesh-cb1e00a4/result.json`（基线）、`work/public-mesh-6fd07cc2/result.json`、`work/public-mesh-8e9dd80c/result.json`（修复后）；没有使用真实 Codex 凭据或修改正式数据目录。
- 该开发阶段未打包、未发布、未替换当前 EXE；正式构建验证见上。GUI 通道复用经调用路径测试，真实入口启动/退出回归通过，但未将两台物理设备的实际 GUI 匹配当作已验收。

## 开发阶段：重启续读改进（2026-09-06）

- 实现前新增回归出现 2 项失败：已绑定回合跨重启丢失新增 Token；退出期间开始的新回合丢失 Token。
- 修复后完整测试集 111 项通过（113.75 秒）。新增 11 项覆盖重复重启去重、退出期间续读、配置修改、账号/API 切换、缺少旧版记录、重新添加账号、缺少配置版本、未绑定会话、实际 revision 元组持久化及额度区间分摊、采集期间账号切换作废续读证据。
- 只在已验证的账号/配置版本/日志目录/设备/添加时间保持一致时续读；首次旧版升级仍保守建立基线。未放宽已有账号隔离，未回填历史。
- 该开发阶段仅源码与隔离自动化测试；正式构建验证见上；未替换运行中 EXE，未在另一台物理设备验收。

## 0.2.1 状态界面验证

- 100 项自动化测试通过（102.22 秒）：包含新增的准备中／超时保留、保存不等于连接、连接不等于收到同步、断线／陈旧回执不显示成功、账号切换清除回执。
- 新增真实 main.py 入口退出回归：持久匹配码文本框曾因全局取消子控件定时器而导致退出异常；已移除普通退出和更新退出中的错误清理，改由控件自行销毁。回归由退出超时失败变为正常退出。
- 收到同步时间由 Engine 在验证账号并成功处理消息后更新。错误账号和无效账本数据不会产生同步成功回执。
- 真实 GUI 事件：生成代码后页面保留、复制按钮读取剪贴板一致；后台 render 不覆盖生成与超时状态；接收保存状态、等待首次同步、已收到同步均逐项断言。
- 真实 GUI 定时回调：后台忙碌也不能无限延长生成等待；旧请求回调不能覆盖新的接收状态。
- 最终 0.2.1 EXE：演示启动／正常退出、未添加账号后台启动、已添加真实账号只读周额度查询、打包中转加密传输全部通过；冻结模块、内置连接组件、更新助手与源构建一致。本轮未重新执行完整自动更新替换链路。
- UI 预览为明确标记的演示数据；本轮没有把三台异地机器的实际连接当作已验收，也未复现用户当时的具体网络失败原因。
- 0.2.1 正式版发布构建；使用现有发布密钥签署更新清单，支持 0.2.0 内置更新。不会主动启动或替换本机正在运行的软件。

## 0.2.0 新增实测

- 两个隔离客户端经真实公共中转连接，TLS 1.3、未知设备 HMAC 自动配对、加密邮箱传输成功；facts 与 sync 两个独立槽位都到达。
- 真实 FileMesh 接收端关闭后重启，保持设备身份、自动重连并再次收到加密消息。数据均为测试合成，不含用户账号或 Token。
- 新的三 Engine + 三本地 Syncthing 进程集成：4000 合成 Token 汇总、12 个百分点分摊、本机限额修改同步、第三节点重启后账本与连接恢复；不同账号拒绝解密。
- 无参数 GUI 发送路径实际生成 CQG2，未展开高级服务器表单；设置页检测更新按钮、默认自动更新和演示模式禁止安装均通过。
- 更新助手 EXE + 两套隔离 Windows 测试 EXE：验证发布密钥签名、等待原进程退出、原地替换、自动启动与健康回执；新版故意退出时自动恢复旧 EXE 并重启。
- 前期公网测试确实失败过：上游代理连接包装类型被 TCP 参数设置拒绝、首次发现负缓存长延迟、部分公共中转超时。分别通过小范围 native 补丁、匹配码自动地址提示与重试处理；不掩盖公共中转仍有不稳定性。
- 仍未在三台物理异地 Windows 主机同时验收；公网实测是本机两个隔离实例通过系统代理访问公网中转，不等价于任意三种 NAT。
- Ed25519 更新清单签名不是 Windows Authenticode 代码签名。

- 最终冻结包逐模块 code object 与当前源文件一致，内置 Syncthing SHA256 和更新助手字节一致，第三方许可已内置。

- 最终主 EXE：演示启动、未添加账号后台启动、明确添加真实账号后的只读周额度查询、独立更新健康回执均通过。
- 最终更新助手重测：原地替换／重启、故意失败回滚、真实 PyInstaller onefile 主 EXE 自然退出后的原地替换全部通过；均使用隔离数据目录，不注册测试自启动，不修改用户登录。

## 0.1.2 保留回归证据

- 原始活动缺陷先复现：启动前识别 1，启动边界后错误变 0。修复后同一测试通过；不放宽 Token 账号归属条件。
- 用户正在运行的本机数据库只读备份回放：修复前当前账号活动为 0；修复后检测到 1 个近期活动、归属待确认 1、当前账号归属 0、无新待上传 Token。未修改真实数据库或会话文件。
- 新增活动回归：0.1.1 游标恢复、当前账号隔离、尾部 token_count 不重开已结束会话、过期回合完成事件不关闭新回合、待归属活动经真实 Journal.presence 同步且不入账。
- 新增 7 种受限后切换回归：自定义 Provider、profile、自定义 OpenAI base_url、API Key、未添加账号、另一已添加账号、退出登录。真实文件身份读取与账本达到上限，防火墙为替身；全部自动恢复且保持原账号账本，切回原账号重新限制。
- `tests/windows_account_switch_probe.py`：真实 Windows 临时进程先暂停＋测试专属防火墙限制，再改隔离 config.toml 为 API，通过 Engine.step 自动恢复。文件心跳继续增长，暂停记录和测试规则已清除。未对真实 Codex 断网或暂停。
- 实机只读自动查找：检测到当前安装目录的 Codex 后台 EXE；不打印或读取进程命令行中的凭据。
- 真实 GUI 事件：启用边框、关闭／受限／API 状态、拖动和点击滑条、直接键入、越界禁止保存、保存／取消、忙碌时防止旧状态覆盖、额度未知时仍显示活动。截图为演示数据。
- 本次 Windows 实测未切换用户真实 Codex 登录／API 配置；临时配置链路通过不等于实际 CC Switch 整套切换已经人工验收。

## 已验证

- `pytest tests -q`：93 项通过（2026-09-06 最终构建前完整运行，89.80 秒）。覆盖采集去重、缓存计数、明确添加账号、A → API → B → A 切换、查询/采集期间切换、账号独立配额/周期、额度分摊、乱序和离线补传、配对码/DPAPI、篡改与重放拒绝、刷新确认、限制/恢复状态机、持续查询失败时的一次性提醒。
- 三个真实本机客户端通信实例：WebRTC DataChannel 直连、加密 WebSocket 备用传输、发现服务中断后的已有直连、服务恢复重连。
- 三个 Engine 与真实通信层联调：合并 4000 个合成 Token；账号 12 个百分点增量按 2:1:1 分摊为 6/3/3；设备自己设置配额并同步；重启保留配对账本。接收端拒绝替其他设备修改配额。
- 三个设备账本刷新测试（合成官方快照，防火墙替身）：按时刷新、提前刷新、重置卡 x1 后刷新。新周期 ID 一致，各设备保留 10/20/30 上限，周期 Token 和已用归零、自动恢复，自动限制保持启用。第三台离线时不伪造同步，补传后恢复；另一个账号不受影响。
- 重置卡请求本身、未确认的首次回退快照均不清零；只改变重置后的官方周快照，不实际兑换任何卡。
- 日/ISO 周/月 Token：跨年自然周分组、跨月分组、账号/设备隔离、重启持久化、额度重置不清除历史。
- 真实 GUI：总览/配对/设置页，发送与接收匹配码、持久化保存、保留本机设备 ID。1240×840 与 1100×720 窗口预览；小窗口支持滚动访问底部按钮。
- 真实本机 Codex 只读测试：识别账号、读取本地日志和官方周额度；未修改登录凭据，未限制正在工作的 Codex。
- 打包客户端：演示模式启动/退出；未添加账号时后台启动、不产生使用事实；明确添加真实账号后只读启动，SQLite 存在有效周额度快照。自动限制默认关闭；测试启动不注册正式自启动。
- 打包中转 EXE：本机启动、两个连接发现、加密消息传输。
- 真实 Windows 限制原语：仅对测试创建的临时 EXE 添加专属防火墙规则、暂停、重复暂停、恢复；心跳证明暂停/恢复有效；规则已清理。
- 恢复 EXE：验证 `requireAdministrator` manifest；恢复逻辑另有状态机测试和临时进程测试。
- 真实 Windows 托盘：点击 X 对应路径隐藏窗口，监测保持运行且限制不被解除；托盘菜单恢复窗口、退出程序。
- 自启动：默认启用的设置测试；真实临时 HKCU Run 注册项启用/关闭、临时最高权限本用户登录任务创建/核对/删除。所有临时项已清理，未注册开发工作目录为用户正式自启动。
- 后台模式：隐藏后不重绘界面、通常约 30 秒检查；受限时约 2 秒检查当前配置切换。同伴数据包不逐条唤醒采集。未测量整机功耗，不声称具体瓦数或 CPU 百分比。

## 尚未验证 / 不作保证

- EXE 防火墙与进程暂停并非逐请求账号控制。同一 EXE 内混合订阅／API／多账号并发不保证隔离；只承诺已观测到当前配置切换后执行解除。

- 自动连接已通过公共中转实测；可选的自建 Docker/Caddy 部署仍未在公网主机上线验收。
- 未在另外两台 Windows 机器上实测；不同 NAT、sing-box、VPN、网络切换仍需三机联调。未真实注销/重启 Windows 验收登录触发。
- 未对用户正在使用的 Codex 实施真实自动断网/暂停，也未等待真实一周刷新或消耗提前重置次数；对应逻辑使用合成额度快照测试。
- 没有官方逐设备周额度扣费接口；设备百分比是估算，不保证固定误差或精确在 33% 停止。
- 不保证只凭匹配码、没有可用发现/中转服务，便能跨任意公网建立连接。
- EXE 未做代码签名，未在干净 Windows 虚拟机上验收全部系统依赖。

预览截图使用明确标注的演示数据，不代表真实账号的消耗量。
# 0.2.3 本机归属修复验证 — 2026-09-07

- 最终完整回归：`python -m pytest tests -q`，**155 passed**（`work/tests-0.2.3-final.log`）；放宽条件后仍保留账号/API 切换隔离断言，没有删除这些保护测试。
- 实际旧账本复现：本周期 35 个百分点未归属，其中 34 个百分点的时间段无已入账 Token，1 个百分点涉及多设备 `codex-auto-review` 未知权重。不是 35% 的已采集 Token 没有设备 ID。
- 新增独立数值历史扫描、额度快照关联、连续会话单锚点／双端锚点推断、账号／Provider／配置变更边界，以及带来源证据的幂等补传。
- 定向回归覆盖旧版 seed、高水位、重启、账号/API 切换、拒绝记录、同会话冲突、分页尾部、部分行、归档移动、跨设备收敛、刷新时间抖动和包含长标识/锚点的批量补传。
- `work/packaged-recovery-20260907-020845/result.json`：冻结 EXE 使用真实账本副本启动，禁用配对、自动限额及自启动；自动完成补记，未归属降至 28%。原始 Codex 文件只读；没有用演示数据代替迁移验证。
- GUI 演示交互验收：`work/ui-preview-0.2.3/`；真实副本界面：`work/packaged-recovery-20260907-020845/actual-upgraded-gui.png`。
- 最终程序执行 `scripts/verify_package.py`，冻结模块、内置连接组件和更新助手与源码/指定二进制一致。
- 已安装到原运行位置 `D:\Codex\dist\0.2.0\Codex配额管家.exe`，窗口版本为 0.2.3，保持原启动路径和用户数据目录。分发 EXE 位于 `D:\Codex\dist\0.2.3\Codex配额管家.exe`。
- 更新前验证自动限额关闭、没有暂停的 Codex 进程；只重启配额管家。原 EXE、SQLite 一致性备份和设置保存在 `work/runtime-upgrade-0.2.3-20260907-021337/`。
- 真实安装后快照：自动补记 **141 条 / 18,326,632 Token**，本机待核对 **0**；账号已用 **67%**，未归属 **28%**，本机估算 **24.254058710830858%**，另一设备 **14.745941289169142%**。这些是当时快照，后续继续使用会变化。
- 本次没有发布 GitHub Release、推送或签名发布更新清单。其他设备运行新版后会自动扫描自身历史；尚未在实际远端设备上升级验收。剩余 28% 不冒充已经找到归属，也不按现有设备比例强行抹平。

# 0.2.4 运行实例归属验证 — 2026-09-07

- 最终完整回归 **184 passed**，`work/tests-0.2.4-final.log`。包含本次 10 项运行证据测试与另一任务已完成的账号 Token 卡片测试；没有忽略失败用例。`git diff --check` 通过。
- 运行归属覆盖跨会话继承、PID 复用与启动 UUID 隔离、旧进程尾部、不重启身份变化、账号／Provider 冲突、配置切换但没有通知的 API 回合、分批索引、游标重启、不持久化日志正文，以及真实 Engine 在当前 API／B 时补回旧 A 请求且不查询／连接 A。
- `account/updated` 只作为变化边界，现有本机日志不包含其账号内容。本机保留日志中未找到此通知，因此不重启切换路径仅做合成夹具验证；没有切换用户真实登录。一个回合可能发起多个请求，实际身份变化后、缺乏逐请求关联的同回合用量保持待核对，不冒充旧请求尾部。
- 真实源只读核对：`work/runtime-index-probe-20260907-024151/result.json`。从真实账本副本重建，2 次增量扫描，约 0.54 秒；474 条数值事件均补齐 `turn_id`，366 条关联到 4 个保留运行实例。源日志打开为 SQLite `mode=ro`；其余事件未因缺少运行证据丢弃既有归属。
- 冻结程序真实副本自动迁移验证通过：`work/packaged-recovery-20260907-024205/result.json`，GUI 为同目录 `actual-upgraded-gui.png`。关闭该副本的配对、自启动和自动限制；没有用演示数据代替迁移。卡片及运行补记状态在真实 GUI 可见。
- `scripts/verify_package.py` 通过：冻结 Python 模块、内置连接组件、更新助手及许可证匹配；包含已完成的账号 Token 估算卡片。
- 已安装到原启动路径 `D:\Codex\dist\0.2.0\Codex配额管家.exe`，窗口已验证为 0.2.4。分发程序 `D:\Codex\dist\0.2.4\Codex配额管家.exe`，SHA256 `b68d644716611a037427dae2a82ff137c21ae2a23f8336819045c2cb5b560a46`。
- 安装前确认自动限额关闭、没有暂停进程或限制状态；备份原 EXE、设置与 SQLite 一致性副本到 `work/runtime-upgrade-0.2.4-20260907-024524/`。仅重启配额管家，没有重启、暂停或改动 Codex 登录。
- 安装后真实快照 `installed-result.json` 与 `installed-gui.png`：本机历史累计补记 **152 条 / 19,840,702 Token**，待核对 **0**；运行索引完成，新增运行推断补记 **0**（本机现有缺口已由额度证据补记，不把旧成果计作新推断）。账号已用 **72%**，仍有 **28 个百分点**未归属；本机 45,058,851 Token，另一设备 33,530,010 Token。数值是当时快照，不保证后续不变。
- 其他设备升级后会自动重建数值与运行索引；没有远端升级实测，没有发布 GitHub Release、推送或签名更新清单，不承诺缺失源日志能完全还原。
