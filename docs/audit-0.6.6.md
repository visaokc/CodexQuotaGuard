# 0.6.6 修复审计

基准：已发布0.6.5，commit fc62b1b2dcee9437098f6f29cbe6647a1cf0b333。范围：另一任务未提交改动和用户在本任务追加要求。先规范审计，再需求审计。原始incoming diff保存在本机work目录，未纳入发行包。

## Standards

- 已修复：官方/个人原有两套权重与估算边界不符合AGENTS.md同账号/周期校准约束。共享显示统一使用live_reporting输出，并按各自已确认基数排除已扣事件；预估不写入借贷。
- 已修复：active_models只有共享入口校验，旧直连入口可保存无界数据。两个入口共用校验，最多16个模型，旧数据迁移保留。
- 原始Token与官方快照未改写，固定100/3、原归属、维护起止和到期规则均保留。

## Spec

- 已修复：官方先更新、归属后确认、迁移baseline、部分归属缺口等情况下重复/丢失估算。当前周期累计确认样本校准，不跨待确认reset套旧率。
- 已修复：饼图今日合计误用个人百分比。使用原始额度点除以当前官方可用账号数，单周期100点、双周期200点；纳入同源估算，保持三人分摊和账号1归档范围。
- 已修复：并发模型显示覆盖、已结束模型回退。各会话原始归属正确，新端明确空列表与旧端未上报分开处理。
- 已修复：在线人数偏右、动画太快及方向不统一、个人用量只过滤Token曲线、维护开关位置、维护历史滑块。用户行使用成员色显示个人余额进度。
- 已保留：只有主界面变化数位动画；个人明细和设置静态；维护曲线灰色独立显示，饼图无维护类别；维护开关关闭后普通消费归本人。

发现规范问题2组、需求问题4组，均已修复。最严重问题是已被官方扣除的用量再次预估扣减；对应回归覆盖确认先后与边界。样本积累可降低校准噪声，不保证每次误差单调下降；新周期无样本不编造预测。
